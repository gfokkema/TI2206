package nl.tudelft.ti2206.bubbleshooter.core.bubbles;

import static org.junit.Assert.assertEquals;
import static org.mockito.Matchers.anyObject;
import static org.mockito.Mockito.*;
import nl.tudelft.ti2206.bubbleshooter.core.Board;
import nl.tudelft.ti2206.bubbleshooter.core.bubbles.BubbleBehaviour;

import org.junit.Before;
import org.junit.Test;
import org.mockito.Mockito;

public class BubbleBehaviourTest {
	
	/*
	Board board;
	BubbleBehaviour BBehaviour;
	BubbleBehaviour spy;
	
	/**
	 * Setup intialization and stubbing.
	 *
	@Before
	public void setUp(){
		board = mock(Board.class, RETURNS_MOCKS);
		BBehaviour = new BubbleBehaviour();
		spy = spy(BBehaviour);
		Mockito.doNothing().when(board).depthFirst(anyObject(), anyObject(), anyObject());
	}
	
	/**
	 * Test remove.
	 *
	@Test
	public void testRemove() {
		assertEquals(null, spy.remove(board, 2, 3));
	}
	
	/**
	 * Test getGroup.
	 *
	@Test
	public void testGetGroup() {
		spy.remove(board, 2,2);
		Mockito.verify(spy).getGroup(board, 2);
	}
	*/
}
